# Triage labels

The local planning queue uses the canonical roles: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`. A pending user design choice is `ready-for-human`. `blocked`, `in-progress`, and `complete` are ticket progress states, not additional triage roles.
