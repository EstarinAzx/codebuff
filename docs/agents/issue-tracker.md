# Issue tracker: local planning draft

For the current CBM-01 initiative, `docs/prd.md` owns the spec and `docs/issues/NN-<slug>.md` owns each ticket. This uses preset init's local paths. Do not create a second queue under `.scratch/`.

The fork has an authenticated GitHub remote. Local files are the reversible draft destination for this run; publishing them to GitHub remains deferred in `.claude/vibe.md`. This is not a permanent user decision about the repository's tracker.

Read the referenced ticket in full. A ticket is ready only when its status is `ready-for-agent` and every listed blocker is resolved. Keep progress/evidence in its `Checkpoint` section. Completed tickets use `complete`; dependency waits use `blocked`. Use the canonical triage roles where relevant.

PRs as a request surface: off.
